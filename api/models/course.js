
var courses[];

function course(crn, semester, subject, name, prof, day, time, type) {
  this.crn = crn;
  this.semester = semester;
  this.subject = subject;
  this.name = name;
  this.name = prof;
  this.name = day;
  this.name = time;
  this.name = type;
}

function addCourse(course){
  courses.pop(course);
}
